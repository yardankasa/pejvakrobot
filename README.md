# ربات تلگرام PejvakRobot

ربات تلگرام برای مدیریت و فروش سورس‌کد با سیستم پشتیبانی تیکت.

## 📋 فهرست مطالب

1. [معرفی پروژه](#معرفی-پروژه)
2. [ویژگی‌ها](#ویژگی‌ها)
3. [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
4. [پیکربندی](#پیکربندی)
5. [ساختار دیتابیس](#ساختار-دیتابیس)
6. [نحوه کار ربات](#نحوه-کار-ربات)
7. [ساختار پروژه](#ساختار-پروژه)

---

## معرفی پروژه

PejvakRobot یک ربات تلگرام کامل است که امکان فروش و مدیریت سورس‌کدها را فراهم می‌کند. این ربات شامل سیستم پرداخت، سیستم تیکت پشتیبانی، و سیستم مدیریت کاربران است.

### تکنولوژی‌های استفاده شده

- **زبان:** PHP 7.4+
- **دیتابیس:** MySQL/MariaDB
- **API:** Telegram Bot API
- **درگاه پرداخت:** Zibal

---

## ویژگی‌ها

### ✅ سیستم مدیریت سورس‌کد
- آپلود و مدیریت سورس‌کدها
- انواع مختلف سورس: رایگان، VIP، سکه‌ای، عضوگیری، Stars
- سیستم جستجو و فیلتر

### ✅ سیستم پرداخت
- پرداخت با ریال (Zibal)
- پرداخت با سکه (داخلی)
- پرداخت با Stars تلگرام
- سیستم زیرمجموعه‌گیری

### ✅ سیستم تیکت پشتیبانی
- ایجاد و مدیریت تیکت‌ها
- پاسخ‌دهی توسط ادمین‌ها
- آپلود فایل در تیکت‌ها
- Web App برای مدیریت تیکت‌ها

### ✅ سیستم مدیریت کاربران
- مدیریت موجودی سکه و نقره
- سیستم لایک و واکنش
- گردونه شانس
- سیستم جریمه و پاداش

---

## نصب و راه‌اندازی

### پیش‌نیازها

- PHP 7.4 یا بالاتر
- MySQL/MariaDB 5.7 یا بالاتر
- دسترسی به سرور وب (Apache/Nginx)
- دسترسی به Telegram Bot API

### مراحل نصب

#### 1. کلون کردن پروژه

```bash
git clone https://github.com/yourusername/PejvakRobot.git
cd PejvakRobot
```

#### 2. تنظیم فایل .env

```bash
# کپی کردن فایل نمونه
cp .env-sample .env

# ویرایش فایل .env با اطلاعات واقعی
nano .env
```

مقادیر مهم که باید تغییر دهید:
- `TELEGRAM_BOT_TOKEN`: توکن ربات از @BotFather
- `DB_MAIN_PASS`: رمز عبور دیتابیس اصلی
- `DB_TICKET_PASS`: رمز عبور دیتابیس تیکت
- `SUPER_ADMIN_IDS`: آیدی عددی شما در تلگرام

#### 3. ایجاد دیتابیس‌ها

```sql
-- اتصال به MySQL
mysql -u root -p

-- ایجاد دیتابیس اصلی
CREATE DATABASE IF NOT EXISTS codezedi_pejvaksource 
    CHARACTER SET utf8mb4 
    COLLATE utf8mb4_persian_ci;

-- ایجاد دیتابیس تیکت
CREATE DATABASE IF NOT EXISTS codezedi_pejvakticket 
    CHARACTER SET utf8mb4 
    COLLATE utf8mb4_persian_ci;
```

#### 4. راه‌اندازی خودکار جداول

**مهم:** ربات به صورت خودکار جداول را ایجاد می‌کند!

هنگامی که `bot.php` برای اولین بار اجرا می‌شود:
1. به دیتابیس متصل می‌شود
2. بررسی می‌کند که آیا جدول `users` وجود دارد
3. اگر جدول وجود نداشته باشد، فایل `database_migration.sql` را به صورت خودکار اجرا می‌کند
4. تمام جداول و داده‌های اولیه ایجاد می‌شوند

**نکته:** این فرآیند فقط یک بار اجرا می‌شود. اگر جداول از قبل وجود داشته باشند، migration اجرا نمی‌شود.

#### 5. تنظیم Webhook

```bash
# تنظیم Webhook برای ربات
curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://yourdomain.com/bot.php"
```

یا از طریق مرورگر:
```
https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://yourdomain.com/bot.php
```

#### 6. تست ربات

1. به ربات در تلگرام پیام `/start` بفرستید
2. بررسی کنید که پاسخ دریافت می‌کنید
3. بررسی لاگ‌ها برای اطمینان از عدم خطا

---

## پیکربندی

### ساختار فایل کانفیگ

تمام تنظیمات در فایل `config.php` متمرکز شده است. این فایل:

1. **فایل .env را می‌خواند** و متغیرهای محیطی را تنظیم می‌کند
2. **Constants را تعریف می‌کند** برای استفاده در سراسر پروژه
3. **اتصال به دیتابیس را برقرار می‌کند**

### فایل .env

فایل `.env` شامل تمام تنظیمات حساس است:

```env
# دیتابیس
DB_MAIN_HOST=localhost
DB_MAIN_NAME=codezedi_pejvaksource
DB_MAIN_USER=username
DB_MAIN_PASS=password

# تلگرام
TELEGRAM_BOT_TOKEN=your_bot_token
SUPER_ADMIN_IDS=123456789

# پرداخت
PAYMENT_TARIFF=28900
ZIBAL_MERCHANT_KEY=your_merchant_key
```

### مقادیر از .env در مقابل مقادیر ثابت

**از .env خوانده می‌شوند:**
- تمام اطلاعات دیتابیس
- توکن ربات تلگرام
- آیدی‌های مدیران
- تنظیمات پرداخت
- تنظیمات کانال

**مقادیر ثابت (در config.php):**
- تنظیمات پیش‌فرض (در صورت عدم وجود .env)
- ساختار آرایه `$channel`
- تنظیمات کش

---

## ساختار دیتابیس

پروژه از دو دیتابیس جداگانه استفاده می‌کند:

### دیتابیس اصلی (`codezedi_pejvaksource`)

**جداول اصلی:**
- `users`: اطلاعات کاربران
- `files`: فایل‌های سورس
- `download`: تاریخچه دانلودها
- `buy`: تاریخچه خریدها
- `likes`: لایک‌های کاربران
- `panel`: تنظیمات پنل مدیریت
- `re_payments`: پرداخت‌ها
- `zm_invites`: دعوت‌های سیستم عضوگیری
- `zm_joins`: عضویت‌های سیستم عضوگیری
- `reactions`: واکنش‌های کاربران
- `luckwheel_stats`: آمار گردونه شانس
- `sends`: سورس‌های در انتظار تایید
- `Admins`: اطلاعات مدیران
- `send_all`: تنظیمات ارسال همگانی

### دیتابیس تیکت (`codezedi_pejvakticket`)

**جداول:**
- `users`: کاربران سیستم تیکت
- `tickets`: تیکت‌های پشتیبانی
- `ticket_messages`: پیام‌های تیکت
- `ticket_attachments`: فایل‌های پیوست

### ایجاد خودکار جداول

هنگامی که ربات برای اولین بار اجرا می‌شود:

1. **بررسی وجود جداول:** ربات بررسی می‌کند که آیا جدول `users` وجود دارد
2. **اجرای Migration:** اگر جدول وجود نداشته باشد، فایل `database_migration.sql` اجرا می‌شود
3. **ایجاد جداول:** تمام جداول با ساختار کامل ایجاد می‌شوند
4. **داده‌های اولیه:** داده‌های اولیه (مثل تنظیمات پنل) اضافه می‌شوند

**کد مربوطه در `bot.php`:**
```php
// بررسی و migration دیتابیس اصلی
checkAndMigrateDatabase($pdo, DB_MAIN_NAME);

// بررسی و migration دیتابیس تیکت
checkAndMigrateDatabase($pdoTicket, DB_TICKET_NAME);
```

---

## نحوه کار ربات

### جریان اصلی

1. **دریافت Update:** ربات از طریق Webhook، Update را از تلگرام دریافت می‌کند
2. **پردازش:** `bot.php` Update را پردازش می‌کند
3. **دستورات:** بر اساس نوع Update (پیام، callback، و...) دستور مناسب اجرا می‌شود
4. **پاسخ:** پاسخ مناسب به کاربر ارسال می‌شود

### دستورات اصلی

- `/start`: شروع کار با ربات
- `آمار 📈`: مشاهده آمار (فقط ادمین)
- `پنل`: ورود به پنل مدیریت
- `جستـجو 🔍`: جستجوی سورس
- `حساب من 👤`: مشاهده حساب کاربری
- `پشتیبانی 🆘`: ورود به سیستم تیکت

### سیستم پرداخت

1. کاربر سورس VIP را انتخاب می‌کند
2. لینک پرداخت Zibal برای کاربر ارسال می‌شود
3. کاربر پرداخت را انجام می‌دهد
4. در `PayLink/verify.php` پرداخت تایید می‌شود
5. فایل به کاربر ارسال می‌شود

### سیستم تیکت

1. کاربر از طریق Web App یا دستور `/start` وارد سیستم می‌شود
2. تیکت جدید ایجاد می‌کند
3. ادمین از طریق Web App یا پاسخ به پیام، پاسخ می‌دهد
4. کاربر از طریق Web App پاسخ را مشاهده می‌کند

---

## ساختار پروژه

```
PejvakRobot/
├── bot.php                      # فایل اصلی ربات
├── config.php                   # فایل کانفیگ مرکزی
├── panel.php                    # پنل مدیریت
├── index.php                    # فایل اصلی
├── .env                         # متغیرهای محیطی (نباید commit شود)
├── .env-sample                  # نمونه فایل .env
├── .gitignore                   # فایل نادیده‌گیری Git
├── database_migration.sql       # فایل SQL برای ایجاد جداول
├── README.md                    # این فایل
│
├── PayLink/                     # سیستم پرداخت
│   ├── request.php             # درخواست پرداخت
│   ├── verify.php              # تایید پرداخت
│   └── func_pay.php            # توابع پرداخت
│
├── ticket_system/               # سیستم تیکت
│   ├── api.php                 # API سیستم تیکت
│   ├── bot.php                 # Handler ربات برای تیکت
│   ├── config_ticket.php       # کانفیگ تیکت (از config.php استفاده می‌کند)
│   ├── index.html              # Web App تیکت
│   └── uploads/                # فایل‌های آپلود شده
│
├── data/                        # فایل‌های داده
│   ├── banner.jpg              # بنر اصلی
│   ├── mark.png                # واترمارک
│   └── error.png               # تصویر خطا
│
└── cache/                       # فایل‌های کش (خودکار ایجاد می‌شود)
```

---

## نکات مهم

### 🔒 امنیت

1. **فایل .env را commit نکنید:** این فایل در `.gitignore` قرار دارد
2. **فایل .env-sample را commit کنید:** این فایل بدون اطلاعات حساس است
3. **رمزهای عبور قوی استفاده کنید:** برای دیتابیس و API
4. **HTTPS استفاده کنید:** برای Webhook و Web App

### 📝 لاگ‌ها

لاگ‌های خطا در فایل‌های زیر ذخیره می‌شوند:
- `api.log`: لاگ‌های API تیکت
- `bot_startup_error.log`: خطاهای راه‌اندازی ربات
- `reaction_error.log`: خطاهای سیستم واکنش

### 🔄 بروزرسانی

برای بروزرسانی:
1. از دیتابیس backup بگیرید
2. فایل‌های جدید را آپلود کنید
3. اگر migration جدیدی وجود دارد، آن را اجرا کنید
4. ربات را تست کنید

---

## عیب‌یابی

### مشکل: جداول ایجاد نمی‌شوند

**راه‌حل:**
1. بررسی کنید که فایل `database_migration.sql` وجود دارد
2. بررسی کنید که دسترسی‌های دیتابیس صحیح است
3. لاگ‌های PHP را بررسی کنید

### مشکل: ربات پاسخ نمی‌دهد

**راه‌حل:**
1. بررسی Webhook: `https://api.telegram.org/bot<TOKEN>/getWebhookInfo`
2. بررسی لاگ‌های سرور
3. بررسی اتصال به دیتابیس

### مشکل: پرداخت کار نمی‌کند

**راه‌حل:**
1. بررسی کلید مرچنت Zibal
2. بررسی URL callback در تنظیمات Zibal
3. بررسی لاگ‌های `PayLink/verify.php`

---

## پشتیبانی

برای پشتیبانی و گزارش باگ:
- ایجاد Issue در GitHub
- ارسال ایمیل به تیم توسعه

---

## مجوز

این پروژه تحت مجوز [MIT License](LICENSE) منتشر شده است.

---

**آخرین بروزرسانی:** 2024

